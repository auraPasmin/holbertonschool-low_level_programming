#include "holberton.h"

/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
 
int print_alphabet(void)
{
	char i;

	for (i='a'; i <= 'z'; i++)
	{	
		_putchar(i);
	}

	_putchar(i);

	return (0);
}
